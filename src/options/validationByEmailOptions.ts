/**
 * ValidationByEmailOptions is used by validation service for providing request parameters.
 */
export interface ValidationByEmailOptions {
  /**
   * Gets or sets e-mail address registered to Papara.
   */
  email: string;
}
