/**
 * MassPaymentByReferenceOptions is used by mass payment service for providing request parameters.
 */
export interface MassPaymentByReferenceOptions {
  /**
   * Gets or sets payment reference number.
   */
  reference: string;
}
