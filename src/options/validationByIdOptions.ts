/**
 * ValidationByIdOptions is used by validation service for providing request parameters.
 */
export interface ValidationByIdOptions {
  /**
   * Gets or sets papara User ID.
   */
  userId: string;
}
