/**
 * ValidationByPhoneNumberOptions is used by validation service for providing request parameters.
 */
export interface ValidationByPhoneNumberOptions {
  /**
   * Gets or sets phone number registered to Papara.
   */
  phoneNumber: string;
}
