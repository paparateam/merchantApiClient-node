/**
 * CashDepositGetOptions is used by cash deposit service for providing request parameters.
 */
export interface CashDepositGetOptions {
  /**
   * Gets or sets cash deposit ID.
   */
  id: number;
}
